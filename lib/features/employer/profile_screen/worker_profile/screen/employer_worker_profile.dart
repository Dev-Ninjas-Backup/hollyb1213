import 'package:flutter/material.dart';

class EmployerWorkerProfile extends StatelessWidget {
  const EmployerWorkerProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}